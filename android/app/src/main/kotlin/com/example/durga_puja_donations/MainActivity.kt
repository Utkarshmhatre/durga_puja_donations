package com.example.durga_puja_donations

import io.flutter.embedding.android.FlutterFragmentActivity

// Required for plugins that use AndroidX BiometricPrompt (e.g., local_auth)
// FragmentActivity host avoids "no FragmentActivity" runtime errors.
class MainActivity : FlutterFragmentActivity()
